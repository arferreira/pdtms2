<%= form_for(@soberanium) do |f| %>
  <% if @soberanium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@soberanium.errors.count, "error") %> prohibited this soberanium from being saved:</h2>

      <ul>
      <% @soberanium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
