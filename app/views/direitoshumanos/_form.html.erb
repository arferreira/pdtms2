<%= form_for(@direitoshumano) do |f| %>
  <% if @direitoshumano.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@direitoshumano.errors.count, "error") %> prohibited this direitoshumano from being saved:</h2>

      <ul>
      <% @direitoshumano.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
